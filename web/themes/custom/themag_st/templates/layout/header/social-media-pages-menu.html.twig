{# Header social media pages menu #}

<div class="header__social-menu">
  {% for key, social_page in themag.social_media_pages %}
    <a class="{{ key|clean_class }}" target="_blank" href="{{ social_page.url }}" alt="social media icons">
      {{ social_page.icon|raw }}
      {% if social_page.title|striptags|trim  %}
        <span class="small">{{ social_page.title }}</span>
      {% endif %}
    </a>
  {% endfor %}
</div>
